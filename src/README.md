# src/ — Código del proyecto

Código Python reutilizable del análisis (funciones puras, helpers). Los notebooks y `sandbox/`
consumen esto; lo que se estabiliza aquí es la "biblioteca" del proyecto.

- `utils/` — utilidades transversales (limpieza, formateo) sobre **pandas**.
